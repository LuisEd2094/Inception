version: '3.8'

services:
  nginx:
    build:
      context: ./requirements/nginx/
      dockerfile: Dockerfile
    image: nginx
    ports:
      - 8080:443
  mariadb:
    build:
      context: ./requirements/mariadb/
      dockerfile: Dockerfile
    image: mariadb
    env_file: ./requirements/mariadb/conf/.env
